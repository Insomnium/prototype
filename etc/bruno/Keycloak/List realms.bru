meta {
  name: List realms
  type: http
  seq: 2
}

get {
  url: http://localhost:9090/admin/realms
  body: none
  auth: inherit
}

script:pre-request {
  const accessToken = bru.getEnvVar('keycloak_access_token');
  req.setHeader('Authorization', 'Bearer ' + accessToken);
}
